{% extends './base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <style>
        .table-primary tbody+tbody, 
        .table-primary td, 
        .table-primary th, 
        .table-primary thead th {
            border-color: #0bb0da;
            color:#fff;
        }        
        
        .table-primary, 
        .table-primary>td, 
        .table-primary>th,
        .table-hover .table-primary:hover,
        .table-hover .table-primary:hover > td,
        .table-hover .table-primary:hover > th {
            background-color: #0bb0da;
        }        
    </style>
{% endblock %}
{% block body %}
    <div class="container mt-5">
        {% include './flashMessages.html.twig' %}
        <div class="row">
            <div class="col-md-12 col-sm-12">
                <p class="lead">Wynik obliczeń</p>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12 col-sm-12">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                          <tr class="table-primary">
                            <th>Dane wejściowe</th>
                            <th>Dane wyjściowe</th>
                          </tr>
                        </thead>
                        <tbody>
                            {% for input, output in sequenceResult %}
                                <tr>
                                  <th>{{ input }}</th>
                                  <td>{{ output }}</td>
                                </tr>                                
                            {% endfor %}
                          </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12 col-sm-12 text-right">
                <a href="{{ path('sequence') }}" class="btn btn-lg  btn-primary">Wróc do zabawy</a>
            </div>
        </div>
    </div>
{% endblock %}